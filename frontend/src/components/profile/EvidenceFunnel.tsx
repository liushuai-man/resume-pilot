import type { ProfileOverview } from '@/api/profile.api';
export default function EvidenceFunnel({ data }: { data: ProfileOverview }) { const items = [['保存 JD', data.funnel.jobDescriptions], ['确认画像', data.funnel.confirmedProfiles], ['岗位匹配', data.funnel.matchAnalyses], ['完成面试', data.funnel.interviewsStarted], ['正式报告', data.funnel.reportsCompleted]] as const; return <section className="rounded-xl border border-[#D8E1DD] bg-white p-6"><h2 className="text-lg font-semibold">闭环完成量</h2><p className="mt-1 text-sm text-[#718079]">来自当前仍保留的正式记录，不等同于行为埋点转化率。</p><div className="mt-5 flex flex-wrap items-center gap-2">{items.map(([label, count], index) => <div key={label} className="contents"><div className="min-w-28 rounded-lg bg-[#F4F7F6] p-3"><p className="text-xs text-[#718079]">{label}</p><p className="mt-1 text-xl font-semibold">{count}</p></div>{index < items.length - 1 && <span className="text-[#9AABA3]">→</span>}</div>)}</div></section>; }
