import type { ProfileOverview } from '@/api/profile.api';
const confidence = { low: '低置信度', medium: '中等置信度', high: '高置信度' };
export default function CapabilityProfile({ data }: { data: ProfileOverview }) { return <section className="rounded-xl border border-[#D8E1DD] bg-white p-6"><h2 className="text-lg font-semibold">能力画像</h2><p className="mt-1 text-sm text-[#718079]">只聚合正式面试评价，并同时展示样本依据。</p>{data.capabilityProfile.length ? <div className="mt-5 space-y-4">{data.capabilityProfile.map((item) => <div key={item.key}><div className="mb-2 flex justify-between gap-3 text-sm"><span className="font-medium">{item.label}</span><span>{item.score} 分 · {confidence[item.confidence]}</span></div><div className="h-2 rounded-full bg-[#E4EAE7]"><div className="h-full rounded-full bg-[#176B52]" style={{ width: `${item.score}%` }}/></div><p className="mt-1 text-xs text-[#718079]">{item.interviewSamples} 场面试 · {item.evidenceCount} 道题证据</p></div>)}</div> : <div className="mt-6 rounded-lg bg-[#F4F7F6] p-5 text-sm text-[#66736D]">至少完成 {data.capabilityMinimumSamples} 场正式评价后生成长期能力画像。单场结果仍可在面试记录中查看。</div>}</section>; }
