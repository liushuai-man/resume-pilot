import type { ProfileOverview } from '@/api/profile.api';
type Metric = { label: string; value: string | number; unit?: string; primary?: boolean };
export default function ProfileMetrics({ data }: { data: ProfileOverview }) { const metrics: Metric[] = [{ label: '完成面试', value: data.interview.count, unit: '场', primary: true }, { label: '面试平均分', value: data.interview.averageScore ?? '-', unit: data.interview.averageScore == null ? undefined : '分', primary: true }, { label: '最新 ATS 内容分', value: data.resumeQuality.latestContentScore ?? '-', unit: data.resumeQuality.latestContentScore == null ? undefined : '分' }, { label: '最新岗位匹配', value: data.resumeQuality.latestMatchScore ?? '-', unit: data.resumeQuality.latestMatchScore == null ? undefined : '分' }, { label: '已采纳优化', value: data.resumeQuality.acceptedOptimizations, unit: '条' }, { label: '平均优化变化', value: data.resumeQuality.averageScoreDelta == null ? '-' : `${data.resumeQuality.averageScoreDelta >= 0 ? '+' : ''}${data.resumeQuality.averageScoreDelta}`, unit: data.resumeQuality.averageScoreDelta == null ? undefined : '分' }]; return <section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{metrics.map((metric) => <div key={metric.label} className={`rounded-xl border bg-white p-5 ${metric.primary ? 'border-[#BFD3CA]' : 'border-[#D8E1DD]'}`}><p className="text-xs text-[#718079]">{metric.label}</p><p className={`mt-2 text-[#17211D] ${metric.primary ? 'text-xl font-semibold' : 'text-lg font-medium'}`}><span>{metric.value}</span>{metric.unit && <span className="ml-1 text-xs font-normal text-[#8A9691]">{metric.unit}</span>}</p></div>)}</section>; }
