import type { ProfileOverview } from '@/api/profile.api';
export default function PositionProfiles({ data }: { data: ProfileOverview }) { return <section className="rounded-xl border border-[#D8E1DD] bg-white p-6"><h2 className="text-lg font-semibold">按岗位观察</h2><p className="mt-1 text-sm text-[#718079]">不同 JobProfile 分开统计，通用岗位不会混入定向结论。</p><div className="mt-4 space-y-3">{data.positionProfiles.map((item) => <div key={item.key} className="rounded-lg border border-[#E1E7E4] p-4"><div className="flex justify-between"><span className="font-medium">{item.label}</span><span className="font-semibold text-[#176B52]">{item.averageScore} 分</span></div><p className="mt-1 text-xs text-[#718079]">{item.interviewCount} 场正式面试{item.capabilityProfile.length ? ` · ${item.capabilityProfile.length} 个可聚合维度` : ' · 样本不足，暂不生成岗位能力画像'}</p></div>)}</div></section>; }
